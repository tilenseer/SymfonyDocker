# SymfonyDocker
Simple Symfony Docker configuration with Symfony cli, PHP 8.1, Mysql 8 and Nginx 

Just run `docker-compose up -d --build` to start the containers. To use the symfony cli, just exec the php container `docker exec -it php bash`.
